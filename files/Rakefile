# -*- coding: utf-8 -*-
$:.unshift("/Library/RubyMotion/lib")
require 'motion/project/template/ios'
require "rubygems"
require 'bundler'
Bundler.require

Motion::Project::App.setup do |app|
  # Use `rake config' to see complete project settings.
  app.name = 'motion_sw_reveal-tutorial'
  app.provisioning_profile = '/Users/ctslin/Copy/_mac/ios_development_devices.mobileprovision'
end
